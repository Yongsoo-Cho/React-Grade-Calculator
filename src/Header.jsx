import React from 'react';
import './main.css'

const Header = () => {    
    return(
        <div className='header'>
            <h1>Grade Calculator in React</h1>
        </div>
    );
};

export default Header;